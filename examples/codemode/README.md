# codemode

Example showing an agent using `shfs/code-mode` to run TypeScript against an
`shfs` filesystem with normal Node.js filesystem APIs.

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```
